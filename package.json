{
  "name": "redux-reconnecting-socket",
  "version": "0.5.0",
  "description": "Typescript Redux socket",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc",
    "npm-install-peers": "npm-install-peers",
    "test": "npm install --no-save redux && jest --config=jest.config.json",
    "test:watch": "npm install --no-save redux && jest --config=jest.config.json --watch"
  },
  "author": "Thomas Kuipers",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "jest": "^23.6.0",
    "npm-install-peers": "^1.2.1",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  },
  "peerDependencies": {
    "redux": "^4.0.1"
  },
  "dependencies": {
    "reconnecting-websocket": "^4.1.10"
  },
  "repository": "dutchsec/redux-reconnecting-socket",
  "keywords": [
    "redux",
    "socket",
    "websocket",
    "reconnect",
    "reconnecting",
    "react"
  ]
}
